# Version 030

Status: success
Type: outbound proxy integration
Triggered by: explicit user request

## Scope

- Add one shared outbound proxy transport for GitHub, Trending, OSS Insight, OpenSSF, and ecosyste.ms requests.
- Support HTTP, HTTPS, SOCKS5, and SOCKS5H proxy URLs.
- Use the Sub2API loopback gateway through `FETCHGITHUB_PROXY_URL` without copying subscription parsing logic.

## Candidate

- Branch: `release/030 @ 92036d3f1ede5acabbfe649fef274609b4afc95c`
- Deployment commit: `92036d3f1ede5acabbfe649fef274609b4afc95c`
- Server path: `/home/ubuntu/projects/fetch-github`

## Validation

- `pnpm typecheck`: passed
- `pnpm test`: 70 passed
- `pnpm build`: passed
- Live service and proxy egress verification: passed

## Configuration

```env
FETCHGITHUB_PROXY_URL=socks5://127.0.0.1:32000
```

The real proxy URL remains in the server environment and is not committed.

## Production result

- `fetchgithub-web.service`: active
- `fetchgithub-worker.service`: active
- Root URL: HTTP `307` to login
- Unauthenticated dashboard API: HTTP `401`
- Sidecar gateway egress probe: returned an IP through `127.0.0.1:32000`

## Rollback

- Roll back to the previous successful fetchGithub commit recorded in the prior release record.
- Do not restore database dumps unless separately authorized and planned.
