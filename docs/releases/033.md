# Version 033

Status: in progress
Type: feature and bugfix
Triggered by: explicit user request

## Scope

- Add Provider-scoped outbound proxy environment checks.
- Test every proxy URL from `.env.local` / `.env` against the selected Provider hostname only, without invoking models or sending API keys.
- Show proxy endpoint, connectivity status, HTTP status, and elapsed time in the Provider dialog.
- Improve Provider action layout and add a database connection timeout for local JSON fallback.

## Candidate

- Branch: `main`
- Base commit: `5154cfc`
- Candidate release commit: pending
- Server path: `/home/ubuntu/projects/fetch-github`

## Validation

- `pnpm typecheck`: pending
- `pnpm test`: pending
- `pnpm build`: pending
- `git diff --check`: pending

## Deployment

- Database migration: not applicable; no schema changes.
- Backup: not applicable; no database data changes.
- Deployment commit: pending
- Database initialization: pending
- Production build: pending
- Web/worker status: pending

## Live verification

- Server commit: pending
- Root URL: pending
- Unauthenticated dashboard API: pending
- Provider proxy-check API: pending; requires authenticated session and configured proxy variables.

## Rollback

- Previous successful deployment: `5154cfc` / `v032`.
- Roll back application code to the previous successful commit if deployment verification fails.
- Do not restore database data; this release has no database migration.

## Known risks

- Proxy checks use a `HEAD /` request to the Provider origin; some gateways may reject HEAD even when the host is reachable.
- Live proxy results depend on the server's configured environment variables and upstream network state.

## Release closure

- `draft`: completed
- PR: not-applicable
- `merged`: pending
- `deployed`: pending
- `live verified`: pending
- `knowledge closed`: pending
- `cleaned`: pending
