# 版本 009

状态：成功
类型：功能与文档
触发人：用户
是否涉及数据库：否
是否涉及 worker：否
是否涉及生产部署：是

## 目标

- 将持续扫描机制和扫描周期的用户界面职责落实到产品文档。
- 将扫描周期恢复能力并入 `System Operations`，移除一级 `Scan Jobs` 导航入口，同时保留旧 `/jobs` 路径兼容。
- 修复 AI Provider 表格长名称、长模型名导致的列挤压和操作区布局问题。

## 初始基线

- branch：`main`
- commit：`a25ba90`
- 上一个成功版本：`v008`

## 变更内容

- 更新 PRD、MVP、技术设计、项目 Agent 规则和本地配置文档，反映固定唤醒周期、自动 Provider 选择、本地知识库 adapter 和成本 dashboard 的当前实现。
- 将扫描周期、暂停/恢复、异常处理和归档能力放入 `System Operations`。
- `/jobs` 自动重定向到 `/operations`，避免历史书签失效。
- Provider 名称和模型名称超长时显示省略号，并保留完整值的 tooltip。

## 风险与回滚

- 风险：旧 `/jobs` 页面不再作为独立导航入口；后端 `/api/scans` 和任务恢复 API 保持不变。
- 回滚目标：`v008`
- 数据库备份：本版本不修改 schema，无额外数据库迁移。

## 验证

- `pnpm typecheck`：通过
- `pnpm test`：通过，60 项
- `pnpm build`：通过，Next.js 16.2.7 生产构建成功
- `git diff --check`：通过
- 线上验证：通过未登录路径和服务状态检查

## 部署信息

- 部署 commit：`a692102`
- 服务器路径：`/home/ubuntu/projects/fetch-github`
- `pnpm db:init`：本版本不涉及 schema，按发布流程确认无需迁移
- `pnpm build`：通过
- `fetchgithub-web.service`：active
- `fetchgithub-worker.service`：active，重启后正常 bootstrap

## 线上验证

- `/login`：307，未登录跳转 `/login?from=%2F`
- `/operations`：307，未登录跳转 `/login?from=%2Foperations`
- `/jobs`：307，未登录跳转 `/login?from=%2Fjobs`；登录后由应用重定向到 `/operations`
- `/api/dashboard`：401，未登录鉴权正确
- worker 日志：bootstrap 正常；既有 Provider `401 INVALID_API_KEY`、Embedding `400` 和超时失败仍存在，未由本版本引入

## 结果

- 部署结果：成功
- tag：`v009`（已创建并推送）
- 备注：本版本不修改数据库 schema；生产现有 AI Provider 凭据问题需后续在 AI 服务页面检测并恢复或更新密钥。
