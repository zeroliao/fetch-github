# 版本 009

状态：已提测
类型：功能与文档
触发人：用户
是否涉及数据库：否
是否涉及 worker：否
是否涉及生产部署：是

## 目标

- 将持续扫描机制和扫描周期的用户界面职责落实到产品文档。
- 将扫描周期恢复能力并入 `System Operations`，移除一级 `Scan Jobs` 导航入口，同时保留旧 `/jobs` 路径兼容。
- 修复 AI Provider 表格长名称、长模型名导致的列挤压和操作区布局问题。

## 初始基线

- branch：`main`
- commit：`a25ba90`
- 上一个成功版本：`v008`

## 变更内容

- 更新 PRD、MVP、技术设计、项目 Agent 规则和本地配置文档，反映固定唤醒周期、自动 Provider 选择、本地知识库 adapter 和成本 dashboard 的当前实现。
- 将扫描周期、暂停/恢复、异常处理和归档能力放入 `System Operations`。
- `/jobs` 自动重定向到 `/operations`，避免历史书签失效。
- Provider 名称和模型名称超长时显示省略号，并保留完整值的 tooltip。

## 风险与回滚

- 风险：旧 `/jobs` 页面不再作为独立导航入口；后端 `/api/scans` 和任务恢复 API 保持不变。
- 回滚目标：`v008`
- 数据库备份：本版本不修改 schema，无额外数据库迁移。

## 验证

- `pnpm typecheck`：待执行
- `pnpm test`：待执行
- `pnpm build`：待执行
- `git diff --check`：待执行
- 线上验证：待部署

## 部署信息

- 部署 commit：待部署
- 服务器路径：`/home/ubuntu/projects/fetch-github`
- `pnpm db:init`：本版本不涉及 schema，按发布流程确认无需迁移
- `pnpm build`：待部署
- `fetchgithub-web.service`：待验证
- `fetchgithub-worker.service`：待验证

## 线上验证

- `/login`：待验证
- `/operations`：待验证
- `/jobs`：待验证重定向到 `/operations`
- `/api/dashboard`：待验证未登录返回 `401`
- worker 日志：待验证

## 结果

- 部署结果：待部署
- tag：待创建
