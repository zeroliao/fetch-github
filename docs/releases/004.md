# 版本：004

状态：开发完成
类型：功能 / 修复 / 体验
触发人：用户
是否涉及数据库：是
是否涉及 worker：是
是否涉及生产部署：是

## 目标

- 完善推荐列表的喜好、标签、状态筛选和语义搜索闭环。
- 完善 AI 配置新增、修改、启停、删除和连接测试闭环。
- 完善扫描任务观测：失败原因、失败项目数、项目/扫描 Token 汇总。
- 支持暂停任务直接手动完成，进入终态。

## 初始基线

- branch：main
- commit：9b07d0d
- 上一个成功版本：003

## 变更内容

- 推荐项目新增喜欢/不喜欢交互、默认未表态筛选、标签列和标签弹窗。
- 推荐列表新增项目序号、分数和 stars 列排序、机会/分组动作/状态筛选、语义搜索。
- AI 配置支持弹窗新增、修改、启用、停用、删除和测试。
- 发现配置保存增加 loading、成功和失败提示。
- 运行观测提示可关闭，增加失败原因模块、项目 Token 汇总、扫描 Token 汇总。
- LLM 作业记录 `job_id`、失败原因和 token usage；provider 未返回 usage 时显示“未知”。
- 扫描任务增加失败项目数，并支持暂停/待重试任务手动完成。
- 数据库增加 `recommendations.tags_json`、`candidate_queue.error_message`、`discovery_jobs.failed_candidate_count`、`llm_jobs.job_id`、`llm_jobs.error_message`。

## 风险与回滚

- 风险：涉及 schema、扫描任务状态、运行观测和推荐反馈交互，部署后需要执行 `pnpm db:init` 并重启 web/worker。
- 回滚目标：9b07d0d
- 数据库备份：本次为追加字段和幂等迁移，不做破坏性变更；如需数据库回滚，需要先停止 web/worker 并按生产数据库备份恢复流程执行。

## 验证

- `pnpm.cmd typecheck`：通过
- `pnpm.cmd test`：通过，24 项测试通过
- `pnpm.cmd build`：通过
- `git diff --check`：通过，仅有 Windows LF/CRLF 提示
- 其他：待线上 smoke test

## 部署信息

- 部署 commit：待提交
- 服务器路径：`/home/ubuntu/projects/fetch-github`
- `pnpm db:init`：待执行
- `pnpm build`：待执行
- `fetchgithub-web.service`：待重启
- `fetchgithub-worker.service`：待重启

## 线上验证

- `/login`：待验证
- `/operations`：待验证
- `/api/dashboard`：待验证
- worker 日志：待验证

## 结果

- 部署结果：待部署
- tag：待定
- 备注：本版本包含 schema 追加字段，部署必须执行 `pnpm db:init`。
