# 版本：003

状态：成功
类型：功能 / 修复 / 体验
触发人：用户
是否涉及数据库：否
是否涉及 worker：否
是否涉及生产部署：是

## 目标

- 优化推荐项目列表的信息密度和筛选能力。
- 增加推荐项目语义搜索，并在缺少向量时保留文本召回兜底。
- 将项目中文描述改成“功能 / 用途优先”的说明，避免模板化描述。
- 修复隐藏项目后无法从列表恢复展示的问题。

## 初始基线

- branch：main
- commit：2e76f66
- 上一个成功版本：002

## 变更内容

- 推荐项目列表增加序号、分数列排序、Stars 列排序。
- 推荐项目列表增加机会、分组动作、状态三个独立筛选条件。
- 推荐项目列表增加语义搜索入口，支持 embedding 相似度和文本信号混合召回。
- 隐藏操作改成“移出展示 / 恢复展示”闭环，隐藏项目可通过状态筛选找回。
- 推荐摘要生成逻辑改为按项目功能、用途、能力和目标用户生成中文说明。
- LLM 分析 prompt 要求 summary 输出一句中文功能简介，避免复述英文描述。

## 风险与回滚

- 风险：主要影响推荐列表 UI、推荐搜索接口和中文摘要展示；不涉及 schema。
- 回滚目标：2e76f66
- 数据库备份：不涉及 schema 变更，本次不强制备份。

## 验证

- `pnpm.cmd typecheck`：通过
- `pnpm.cmd test`：通过，23 项测试通过
- `pnpm.cmd build`：通过
- `git diff --check`：通过，仅有 Windows LF/CRLF 提示
- 其他：线上 smoke test 通过

## 部署信息

- 部署 commit：6c2e191
- 服务器路径：`/home/ubuntu/projects/fetch-github`
- `pnpm db:init`：不需要
- `pnpm build`：已执行
- `fetchgithub-web.service`：active
- `fetchgithub-worker.service`：active

## 线上验证

- `/login`：200
- `/operations`：307，未登录跳转登录
- `/api/dashboard`：401，未登录鉴权正常
- `/api/recommendations/search?q=rag`：401，新增接口未登录鉴权正常
- worker 日志：重启成功，无新增启动错误

## 结果

- 部署结果：成功
- tag：待创建
- 备注：本次为 UI 和推荐搜索优化，无 schema 迁移；服务器存在既有未跟踪文件 `.verify-db-state.mjs`，本次未处理。
