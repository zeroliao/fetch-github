# 版本：005

状态：成功
类型：功能
触发人：用户
是否涉及数据库：是
是否涉及 worker：否
是否涉及生产部署：是

## 目标

- 新增可通过 URL 直接访问、无需加入侧边导航的 `/mark` 临时文本工作区。
- 支持在同一资源树中管理目录和文件，并通过右键菜单完成增、删、重命名和导出。
- 提供聚焦内容编辑的界面，编辑区顶部只保留复制和保存操作。

## 初始基线

- branch：main
- commit：042529d
- 上一个成功版本：004

## 变更内容

- 新增 `/mark` 页面，复用现有登录鉴权，未登录访问跳转至 `/login?from=/mark`。
- 新增目录与文件混排资源树，以及根节点、目录和文件的右键菜单。
- 支持新建、重命名、软删除、回收站恢复、永久删除及按目录或文件导出。
- 支持主流文本内容粘贴、编辑、保存和一键复制，单文件上限为 2 MB。
- 新增 `/api/mark`、`/api/mark/files/[id]` 和 `/api/mark/export` API。
- 新增 `mark_directories`、`mark_files` 表及相关唯一索引和查询索引，数据按 `user_id` 隔离。
- 目录导出使用 UTF-8 ZIP，导出上限为 100 MB，无新增第三方依赖。

## 风险与回滚

- 风险：文本内容会增加 PostgreSQL 存储占用；目录与文件操作依赖浏览器右键菜单；部署后必须执行 `pnpm db:init`。
- 回滚目标：v004；新增表和索引可保留，应用回滚不需要执行破坏性数据库回滚。
- 数据库备份：部署前从服务器 `.env.local` 安全加载 `DATABASE_URL` 并执行 `pg_dump`，备份文件权限设为 `600`，不得输出连接串或凭据。

## 验证

- `pnpm typecheck`：通过
- `pnpm test`：通过，30 项测试通过
- `pnpm build`：通过
- `git diff --check`：通过
- `pnpm db:init`：通过
- 其他：Playwright 已在 1440x900 和 390x844 视口验证；右键新建、混排、重命名、目录/文件导出、复制和保存均通过。

## 部署信息

- 部署 commit：d1ae552
- 服务器路径：`/home/ubuntu/projects/fetch-github`
- `pnpm db:init`：通过，schema 已应用
- `pnpm build`：通过，构建包含 `/mark` 与三个 Mark API 路由
- `fetchgithub-web.service`：active，Next.js 16.2.7 启动正常
- `fetchgithub-worker.service`：active，重启后继续输出队列统计

## 线上验证

- `/mark`：307，未登录跳转 `/login?from=%2Fmark`
- `/api/mark`：401，返回中文未登录错误
- 数据库表：`mark_directories`、`mark_files` 均存在
- worker 日志：重启成功，无新增启动错误；既有 provider 参数错误和 LLM 超时队列状态仍在持续统计

## 结果

- 部署结果：成功
- tag：v005
- 备注：部署前备份为 `/home/ubuntu/fetchgithub-backup-20260730T005258.sql`，大小 265851246 bytes，权限 `0600`；服务器原有未跟踪文件 `.verify-db-state.mjs` 保留未修改。
