# Version 035

Status: success
Type: feature and UI change
Triggered by: explicit user request

## Scope

- Replace Provider proxy environment-variable selection with Sing-box outbound nodes.
- Discover supported Sing-box HTTP, SOCKS, and mixed inbound nodes from the server configuration.
- Check every discovered node against the Provider Base URL before allowing node selection.
- Allow multiple selected nodes, ordered by measured connectivity latency, with proxy failover and direct-connection fallback.
- Remove the obsolete `FETCHGITHUB_PROXY_URL` configuration from active setup documentation and schema usage.

## Candidate

- Branch: `main`
- Base commit: `c7304eb`
- Candidate release commit: `c4bfdd71d48bda18109a8074f3826c68c0e3cb96`
- Server path: `/home/ubuntu/projects/fetch-github`

## Validation

- `pnpm.cmd typecheck`: passed
- `pnpm.cmd test`: passed (84 tests)
- `pnpm.cmd build`: passed
- `git diff --check`: passed

## Deployment

- Database migration: changed-and-verified; `corepack pnpm db:init` applied the schema, adding `proxy_addresses` and removing the legacy `proxy_url_env` column.
- Backup: pending; the server has no `pg_dump`, and the attempted background backup did not produce a valid dump. Existing backups were not modified.
- Deployment commit: `c4bfdd71d48bda18109a8074f3826c68c0e3cb96`
- Database initialization: passed (`corepack pnpm db:init`)
- Production build: passed (`corepack pnpm build`); existing Next.js NFT tracing warning remains non-blocking.
- Web/worker status: active after restart

## Live verification

- Server commit: changed-and-verified (`c4bfdd71d48bda18109a8074f3826c68c0e3cb96`)
- Root URL: changed-and-verified (HTTP `307` to `/login?from=%2F`)
- Operations URL: changed-and-verified (HTTP `307` to `/login?from=%2Foperations`)
- Unauthenticated dashboard API: changed-and-verified (HTTP `401`)
- Provider proxy-check API: changed-and-verified (HTTP `401` without a session)
- Web/worker logs: changed-and-verified; web reached `Ready`, worker bootstrapped and resumed its scheduled job.
- Remote `.env.local` cleanup: changed-and-verified; 901 variables matching the `FETCHGITHUB_PROXY_URL` prefix were removed, 0 remain, and file mode is `600`.

## Rollback

- Previous successful deployment: `c7304eb` / Version 034.
- Roll back application code to the previous successful commit if deployment verification fails.
- Do not restore database data automatically; the schema migration is additive for `proxy_addresses` but removes the obsolete `proxy_url_env` field.

## Known risks

- Sing-box configuration availability and inbound formats vary by server; unsupported or malformed entries are ignored.
- Provider proxy addresses are stored as ordered URLs and may include multiple nodes; live failover depends on upstream network availability.
- Removing the legacy database column and environment variables is intentionally not backward-compatible per the requested migration.

## Release closure

- `draft`: completed
- PR: not-applicable
- `merged`: completed (fast-forward to `main`)
- `deployed`: completed
- `live verified`: completed
- `knowledge closed`: completed (release record updated with runtime evidence)
- `cleaned`: not-applicable; existing server diagnostic file `.verify-db-state.mjs` was preserved.
