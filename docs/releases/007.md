# 版本：007

状态：已提测
类型：功能与架构
触发人：用户
是否涉及数据库：是
是否涉及 worker：是
是否涉及生产部署：是

## 目标

- 简化发现策略配置，移除运行时数量、Top K、最大运行分钟和具体模型绑定。
- 按规范化 GitHub 仓库地址去重，`processed/skipped` 终态跳过，`failed/exception` 可重试。
- 根据实时可用内存动态批处理，持续扫描并支持 checkpoint 分页。
- 增加 Provider priority、reasoning effort、不可用状态、恢复操作和同类型模型轮换。
- 将推荐喜好、机会资格、机会阶段拆分为独立状态轴。

## 变更内容

- 新增 `repo_processing`、`scan_provider_states`、`provider_health_events` 和 AI Provider 健康字段。
- Provider 按优先级自动选择；连续 3 次 JSON parse/schema 失败后轮换，模型池耗尽进入 `exception`。
- 401/403、429、API Key 缺失、网络/超时、5xx、参数不兼容单独分类，不计入解析失败次数。
- AI Provider 删除改为软删除，保留历史外键数据。
- GitHub Search 按每页 100 条持续分页至结果结束或 1000 条上游边界。
- 推荐不设数量上限，默认综合分降序；增加 `pending` 喜好操作。
- 更新 dashboard、数据库 schema、技术设计、MVP、本地配置和项目 Agent 规则。

## 本地验证

- `pnpm typecheck`：通过
- `pnpm test`：通过，58 项测试
- `pnpm build`：通过
- `git diff --check`：通过
- `pnpm db:init`：待部署环境验证；本机 PostgreSQL 端口未启动

## 风险与回滚

- 数据库变更均为幂等新增字段、表、索引和回填；不执行破坏性删除。
- 应用回滚目标：`v006`。
- 部署前按服务器 `DATABASE_URL` 执行 `pg_dump`，不输出凭据。
- 如果模型池耗尽，扫描任务进入 `exception`，需恢复 Provider 后手动恢复任务。

## 部署信息

- 部署 commit：`3490dcd`
- 服务器路径：`/home/ubuntu/projects/fetch-github`
- 目标 SSH：`sub2api-cf`
- 服务：`fetchgithub-web.service`、`fetchgithub-worker.service`

## 线上验证

- 代码已推送到 `origin/main`。
- 服务器部署待完成：`sub2api-cf` 的 Cloudflare Access SSH 隧道在认证/key exchange 阶段返回 EOF/reset，尚未执行远端命令。
- 登录页、API 鉴权、服务状态、数据库迁移、worker 日志和核心扫描路径待远端连接恢复后验证。
