# Version 031

Status: success
Type: bugfix
Triggered by: explicit user request

## Scope

- Preserve Provider group connection settings, including `proxyUrlEnv`, when model availability is updated after detection.
- Add regression coverage that confirms model probing reads the configured proxy environment variable.

## Candidate

- Branch: `release/031`
- Initial commit: `1843130`
- Candidate release commit: `afebe88`
- Server path: `/home/ubuntu/projects/fetch-github`

## Validation

- `pnpm typecheck`: passed
- `pnpm test`: passed (71 tests)
- `pnpm build`: passed
- `git diff --check`: passed

## Deployment

- Database migration: not applicable; no schema changes.
- Backup: not applicable; no database data changes.
- Deployment commit: `afebe88`
- Database initialization: passed (`corepack pnpm db:init`)
- Production build: passed (`corepack pnpm build`)
- Web/worker status: active
- Root URL: HTTP `307` to `/login?from=%2F`
- Unauthenticated dashboard API: HTTP `401`
- Live provider probe: pending; the configured SOCKS5 listener is reachable, but the upstream model TLS/network path failed during the real probe.

## Rollback

- Previous successful deployment: `92036d3`.
- Roll back application code to the previous successful commit if deployment verification fails.
- Do not restore database dumps; this release has no database migration.

## Known risks

- Real provider egress depends on the configured proxy endpoint and upstream network availability; this does not block application deployment health checks.
