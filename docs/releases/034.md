# Version 034

Status: success
Type: feature and UI refinement
Triggered by: explicit user request

## Scope

- Refine Provider-scoped outbound proxy reachability checks.
- Check every proxy URL from `.env.local` / `.env` against the current Provider hostname only, without model calls or API keys.
- Add status filtering and elapsed-time sorting to the proxy check dialog.
- Keep Provider actions on one desktop row and align the proxy result table to five columns.

## Candidate

- Branch: `main`
- Base commit: `1851339`
- Candidate release commit: `d7bb559f1cf81e5ab4704233826f813781e28a38`
- Server path: `/home/ubuntu/projects/fetch-github`

## Validation

- `pnpm typecheck`: passed
- `pnpm test`: passed (77 tests)
- `pnpm build`: passed
- `git diff --check`: passed

## Deployment

- Database migration: not applicable; no schema changes.
- Backup: not applicable; no database data changes.
- Deployment commit: `d7bb559f1cf81e5ab4704233826f813781e28a38`
- Database initialization: not applicable; no schema changes.
- Production build: passed (`corepack pnpm build`)
- Web/worker status: active

## Live verification

- Server commit: changed-and-verified (`d7bb559f1cf81e5ab4704233826f813781e28a38`)
- Root URL: changed-and-verified (HTTP `307` to `/login?from=%2F`)
- Operations URL: changed-and-verified (HTTP `307` to `/login?from=%2Foperations`)
- Unauthenticated dashboard API: changed-and-verified (HTTP `401`)
- Provider proxy-check API: pending; requires an authenticated session and configured proxy variables.

## Rollback

- Previous successful deployment: `1851339` / Version 033.
- Roll back application code to the previous successful commit if deployment verification fails.
- Do not restore database data; this release has no database migration.

## Known risks

- Reachability checks use an unauthenticated `GET /` request to the Provider origin; an origin may return an application error while remaining network reachable.
- Live proxy results depend on the server's configured environment variables and upstream network state.

## Release closure

- `draft`: completed
- PR: not-applicable
- `merged`: completed (fast-forward to `main`)
- `deployed`: completed
- `live verified`: completed
- `knowledge closed`: completed
- `cleaned`: not-applicable
