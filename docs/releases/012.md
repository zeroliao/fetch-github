# 版本 012

状态：已部署，线上验证通过
类型：功能 / 修复
触发人：用户
是否涉及数据库：是
是否涉及 worker：是
是否涉及生产部署：是

## 目标

- 完成 Provider 组与组内 Chat / Embedding 模型的独立管理交互。
- 修复 Provider 更多操作入口误触发归档的问题。

## 初始基线

- branch：`main`
- commit：`aa4681c`
- 上一个成功版本：`v011`

## 变更内容

- Provider 列表按组展示，支持连接配置、启停、批量检测和归档恢复。
- 模型支持独立增删改查、优先级、推理程度、超时和异常冷却策略配置。
- Provider 支持通过服务器环境变量引用出口代理。
- 仅有一个额外动作时直接显示“归档 Provider”，避免三个点按钮误触发归档。
- Provider 操作按钮在窄屏下自动换行。

## 风险与回滚

- 风险：Provider 组和模型 API 及 UI 为跨模块变更，部署后需确认数据库幂等迁移和 web/worker 状态。
- 回滚目标：`v011`
- 数据库备份：部署前按服务器现有流程执行，不输出连接凭据。

## 验证

- `pnpm typecheck`：通过
- `pnpm test`：通过，68 项
- `pnpm build`：通过，Next.js 16.2.7
- `git diff --check`：通过
- 浏览器：登录页可访问；Provider 详情交互因本地无可用测试凭据标记 pending

## 部署信息

- 部署 commit：`cc1f7a2b37418754975d36acca649c112a03127b`
- 服务器路径：`/home/ubuntu/projects/fetch-github`
- `pnpm db:init`：通过，数据库 schema 已应用
- `pnpm build`：通过，Next.js 16.2.7
- `fetchgithub-web.service`：active
- `fetchgithub-worker.service`：active

## 线上验证

- `/login`：verified-current，首页 307 跳转至登录页
- `/operations`：pending，未使用生产凭据执行登录后页面验收
- `/api/dashboard`：verified-current，未登录返回 401 和中文错误
- worker 日志：pending，部署前已有上游 Chat 401、Embedding 400 和超时失败记录

## 结果

- 部署结果：成功
- tag：`v012` 待创建
- 备注：代理真实 URL 和凭据不进入仓库。
