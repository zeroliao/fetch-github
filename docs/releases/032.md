# Version 032

Status: success
Type: feature and bugfix
Triggered by: explicit user request

## Scope

- Add JSON and XLSX import/export controls beside AI Provider model creation.
- Raise the per-Provider model limit to 500.
- Persist newly imported models in the JSON-file fallback so they remain after a page refresh or local restart.

## Candidate

- Branch: `release/032`
- Base commit: `1b965ef`
- Candidate release commit: `7294cf3`
- Server path: `/home/ubuntu/projects/fetch-github`

## Validation

- `pnpm typecheck`: passed
- `pnpm test`: passed (77 tests)
- `pnpm build`: passed
- `git diff --check`: passed

## Deployment

- Database migration: not applicable; no schema changes.
- Backup: not applicable; no database data changes.
- Deployment commit: `2ab4966ef473791e2791153e4334faf279c3cb2d`
- Database initialization: passed (`corepack pnpm db:init`)
- Production build: passed (`corepack pnpm build`)
- Web/worker status: active

## Live verification

- Server commit: changed-and-verified (`2ab4966ef473791e2791153e4334faf279c3cb2d`)
- `https://github.zero007.chat/`: changed-and-verified (HTTP `307` to `/login?from=%2F`)
- `https://github.zero007.chat/operations`: changed-and-verified (HTTP `307` to `/login?from=%2Foperations`)
- `https://github.zero007.chat/api/dashboard`: changed-and-verified (HTTP `401`, `{"error":"未登录或登录已过期。"}`)
- Web log: changed-and-verified; Next.js started successfully after restart.
- Worker log: changed-and-verified; worker bootstrap completed with `profiles=1 jobs=10`.
- Existing worker provider failures: out-of-scope; upstream Chat API key, embedding parameter, and timeout failures predate this release.

## Rollback

- Roll back application code to the server commit captured before deployment.
- Do not restore database data; this release has no database migration.

## Known risks

- XLSX import supports the defined model sheet format and standard shared-string workbooks; malformed or oversized files are rejected.
- The 500-model limit increases Provider configuration size and can make batch model checks take longer.

## Release closure

- `draft`: completed
- PR: not-applicable
- `merged`: completed after fast-forward to `main`
- `deployed`: completed
- `live verified`: completed
- `knowledge closed`: completed
- `cleaned`: pending; no branches, worktrees, or server diagnostic files were deleted.
