# 版本 008

状态：成功
类型：功能与界面
触发人：用户
是否涉及数据库：否
是否涉及 worker：否
是否涉及生产部署：是

## 目标

- 将 AI Provider 名称规范化为唯一的 API Key 环境变量名，避免多个 Provider 误用同一密钥。
- 优化 AI 配置表格在桌面和窄屏下的可读性与操作布局。

## 初始基线

- branch：`main`
- commit：`b61ede5`
- 上一个成功版本：`v007`

## 变更内容

- 新增 Provider 名称到 API Key 环境变量名的规范化逻辑和回归测试。
- 创建、编辑 Provider 时自动派生并校验 API Key 环境变量名；名称变化要求重新填写 API Key。
- 调整 AI 配置表格列宽、状态信息和窄屏横向滚动布局。
- 更新 MVP、技术设计、本地配置和功能闭环文档。

## 风险与回滚

- 风险：Provider 名称变化会改变环境变量名，必须重新填写对应 API Key。
- 回滚目标：`v007`。
- 数据库备份：本版本不修改数据库 schema，未执行额外数据库备份；回滚目标为 `v007`。

## 验证

- `pnpm.cmd typecheck`：通过
- `pnpm.cmd test`：通过（60 项）
- `pnpm.cmd build`：通过
- `git diff --check`：通过（仅有 Windows 换行转换提示）

## 部署信息

- 部署 commit：`bd4138c`
- 服务器路径：`/home/ubuntu/projects/fetch-github`
- SSH 目标：`sub2api-cf`
- 服务：`fetchgithub-web.service`、`fetchgithub-worker.service`

## 线上验证

- `https://github.zero007.chat/` 返回 `307` 到 `/login?from=%2F`。
- `https://github.zero007.chat/operations` 返回 `307` 到 `/login?from=%2Foperations`。
- `https://github.zero007.chat/api/dashboard` 未登录返回 `401`。
- `fetchgithub-web.service` 和 `fetchgithub-worker.service` 均为 `active`。
- worker 日志仍显示部署前已有的 Provider `401 INVALID_API_KEY`、Embedding `400` 和超时失败；本版本未修改 Provider 密钥值，需后续单独处理。

## 结果

- 部署结果：成功
- tag：待创建
