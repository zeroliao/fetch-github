# 版本：001

状态：开发中
类型：修复
触发人：用户
是否涉及数据库：否
是否涉及 worker：否
是否涉及生产部署：是

## 目标

- 补齐管理员登录后的修改密码入口。
- 避免只能通过服务器 `.env.local` 手工修改 `ADMIN_PASSWORD_HASH`。

## 初始基线

- branch：main
- commit：508cefa
- 上一个成功版本：无

## 变更内容

- 新增 `POST /api/auth/password`。
- 已登录管理员可以输入当前密码和新密码更新 `ADMIN_PASSWORD_HASH`。
- Dashboard 工具栏新增修改密码按钮。
- 修改密码弹窗完成新密码校验和错误提示。

## 风险与回滚

- 风险：密码更新写入 `.env.local`，需要确保服务器进程有写权限。
- 回滚目标：508cefa
- 数据库备份：不涉及数据库。

## 验证

- `pnpm typecheck`：通过
- `pnpm test`：通过
- `pnpm build`：通过
- `git diff --check`：通过
- 其他：

## 部署信息

- 部署 commit：
- 服务器路径：`/home/ubuntu/projects/fetch-github`
- `pnpm db:init`：不涉及
- `pnpm build`：
- `fetchgithub-web.service`：
- `fetchgithub-worker.service`：

## 线上验证

- `/login`：
- 修改密码 API：
- 修改密码 UI：
- worker 日志：不涉及

## 结果

- 部署结果：
- tag：
- 备注：
