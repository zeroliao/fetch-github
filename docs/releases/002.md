# 版本：002

状态：已提测
类型：功能 / 性能 / 体验
触发人：用户
是否涉及数据库：是
是否涉及 worker：是
是否涉及生产部署：是

## 目标

- 将机会雷达补齐为更完整的发现、分析、推荐、反馈闭环。
- 在不削弱发现能力的前提下，减少重复 AI 调用和无效 token 消耗。
- 优化推荐列表、运行观测、机会验证和项目分组体验。

## 初始基线

- branch：main
- commit：ffe8382
- 上一个成功版本：无

## 变更内容

- 新增全局扫描任务开关，关闭后不再创建自动扫描任务。
- 我的 GitHub 增加每日被动同步。
- 扫描记录增加新增项目、更新项目、未变化项目和候选项目计数。
- 增量扫描跳过未变化且已深度分析的项目，不占用深度分析名额。
- 增加 README 压缩、embedding 缓存、profile embedding 缓存、LLM 缓存和批量 embedding。
- 增加语义门控，低相关项目保留为普通候选，但延后 LLM 分析。
- 增加重新分析 delta prompt，复用历史分析并只关注变化。
- 推荐项目增加机会工作流状态、验证清单、解释列、项目分组和隐藏类似项目。
- 运行观测增加异常提醒和更稳定的右侧模块滚动/折叠体验。

## 风险与回滚

- 风险：涉及 schema、扫描流程、AI 缓存和 worker 调度，线上需要执行 `pnpm db:init` 并重启 web / worker。
- 回滚目标：ffe8382
- 数据库备份：部署前在服务器执行。

## 验证

- `pnpm typecheck`：通过
- `pnpm test`：通过，22 项测试通过
- `pnpm build`：通过
- `git diff --check`：通过，仅有 Windows LF/CRLF 提示
- 其他：待线上验证

## 部署信息

- 部署 commit：ed5fc69
- 服务器路径：`/home/ubuntu/projects/fetch-github`
- `pnpm db:init`：已执行
- `pnpm build`：已执行
- `fetchgithub-web.service`：已重启
- `fetchgithub-worker.service`：已重启

## 线上验证

- `/login`：200
- `/operations`：待验证
- `/api/dashboard`：401（未登录）
- worker 日志：无新增错误

## 结果

- 部署结果：成功
- tag：待创建
- 备注：本次已完成提交、推送、服务器同步、数据库初始化、构建和重启
